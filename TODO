- DNS round-robin url

- multiple streams per connection

- VOD HLS

- dynamic relay

- force pull

- sync exec

- update relay engine; add relay rooms; use closeStream instead of deleteStream; add to stat
