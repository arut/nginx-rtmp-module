- DNS round-robin url

- multiple streams per connection

- VOD HLS

- dynamic relay

- plain on_connect, on_update

- disconnect dead streams

- more RTMP statuses/error (non-existent/already-published stream handling (NetStream.Publish.BadName)/NetStream.Record.Failed)

- force pull

- sync exec
